# 🔐 Система аутентификации на Node.js + PostgreSQL

Простая и безопасная система аутентификации с использованием JWT токенов и PostgreSQL.

## 🛠 Технологии

- **Node.js** (с Express.js)
- **PostgreSQL** (база данных)
- **JWT** (JSON Web Tokens для аутентификации)
- **bcrypt** (хеширование паролей)
- **dotenv** (управление переменными окружения)
- **pg** (клиент PostgreSQL)


## 📋 API Endpoints

| Метод | Endpoint | Описание | Требуется токен |
|-------|----------|----------|----------------|
| `POST` | `/auth/register` | Регистрация нового пользователя | ❌ |
| `POST` | `/auth/login` | Вход в систему | ❌ |
| `GET` | `/api/auth` | Проверка валидности токена | ✅ |

## ⚙️ Установка и запуск

### 1. Клонирование и установка зависимостей
```bash
git clone <ваш-репозиторий>
cd <папка-проекта>
npm install
```
### 2. Запуск проекта
```bash
node index.js
```
P.S. Перед запуском не забудьте запустить PostgreSQL бд
